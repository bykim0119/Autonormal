# AGENTS.md V2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the V1 coding-delegation section of `~/.hermes/workspace/AGENTS.md` with a multi-step rule + reversed boundary bias, so parent agents reliably delegate complex coding tasks to `delegate_task_background` instead of executing them in-turn with `write_file`/`edit_file`.

**Architecture:** AGENTS.md is the workspace bootstrap file injected into every hermes session's system prompt. Edit the one file in-place; no code changes, no test code, no service restarts. Verification is live-only against Discord (this is prompt engineering — deterministic test impossible).

**Tech Stack:** Markdown (AGENTS.md). Discord live verification. `~/migration-backups/` for rollback safety.

**Spec:** `docs/superpowers/specs/2026-05-21-agents-md-v2-design.md`

---

## File Structure

- Modify: `~/.hermes/workspace/AGENTS.md` (lines 132–181 → V2 content)
- Create: `~/migration-backups/agents-md-v2-<ISO-timestamp>.bak` (rollback copy)
- Out of scope: `hermes-agent/toolsets.py`, `_HERMES_CORE_TOOLS`, any code files.

The whole plan is one prompt edit. Each task below is a discrete checkpoint so a reviewer can stop after any task and roll back via the backup.

---

## Task 0: Pre-flight — backup + read current state

**Files:**
- Read: `~/.hermes/workspace/AGENTS.md` lines 132–181 (current V1 coding-delegation section)
- Create: `~/migration-backups/agents-md-v2-<timestamp>.bak`

- [ ] **Step 1: Confirm the V1 section's exact line range**

Run:
```bash
grep -n "Coding Delegation\|^## Red Lines" ~/.hermes/workspace/AGENTS.md
```

Expected output (line numbers may have drifted; record exact start/end):
```
132:## 🧑‍💻 Coding Delegation (`delegate_task_background`)
183:## Red Lines
```

The V1 section is from the `## 🧑‍💻 Coding Delegation` line up to (exclusive) the next top-level heading `## Red Lines`. Confirm both anchors exist before editing.

- [ ] **Step 2: Make a timestamped backup**

Run:
```bash
mkdir -p ~/migration-backups
TS=$(date -u +%Y-%m-%dT%H-%M-%SZ)
cp ~/.hermes/workspace/AGENTS.md ~/migration-backups/agents-md-v2-${TS}.bak
ls -l ~/migration-backups/agents-md-v2-${TS}.bak
```

Expected: file exists, same size as `~/.hermes/workspace/AGENTS.md`.

If the backup directory has prior coder-subagent backups (e.g. `coder-subagent-2026-05-19/`), leave them — different scope.

- [ ] **Step 3: Note rollback command**

If anything goes wrong later, rollback is:
```bash
cp ~/migration-backups/agents-md-v2-<TS>.bak ~/.hermes/workspace/AGENTS.md
```
No restart needed since AGENTS.md is re-read by the next session/turn.

---

## Task 1: Replace V1 coding-delegation section with V2

**Files:**
- Modify: `~/.hermes/workspace/AGENTS.md`

- [ ] **Step 1: Replace the section using Edit tool with exact string match**

Use the Edit tool. `old_string` is the entire V1 section from `## 🧑‍💻 Coding Delegation` through the last line before `## Red Lines` (the line that ends with "위 ACP 가이드와 별도."). `new_string` is the V2 section below.

**old_string** (V1, current content lines 132–181):

```markdown
## 🧑‍💻 Coding Delegation (`delegate_task_background`)

Discord 봇에서 코드 작성·실행 요청이 들어오면 별도의 코더 자식 (Codex CLI) 에게 위임한다. 메인 대화 컨텍스트를 코드 디테일로 채우지 말 것.

### 위임 트리거 (자동)
다음 요청은 `delegate_task_background` 로 **바로** 위임:
- "X 파일/함수/모듈 만들어/추가해/수정해"
- "이 코드 디버깅해줘 / 에러 고쳐줘"
- "테스트 작성/실행해줘"
- "리팩토링/정리해줘"
- 여러 단계 명령 실행이 필요한 작업

### 위임 안 함 (메인이 직접 답)
- 짧은 코드 설명/리뷰 (한 함수 5줄 이내, 변경 없음)
- 개념/디자인 토론, 아키텍처 질문
- 한 줄 syntax 질문
- 코드베이스 *탐색* 만 필요한 질문 (변경 X)

### 호출 패턴 (한 번만)
```
delegate_task_background(
    goal="자기완결적 한 줄 요약 — 자식은 대화 history를 모름",
    context="관련 파일 경로, 에러 메시지, 제약 (간결하게)"
)
```

호출 직후 사용자에게 보낼 응답 한 줄:
> "▶ 코더에게 위임 — 진행은 자동 생성된 스레드 참조"

(앵커 메시지 + Discord 스레드는 자동 생성됨. 메인이 thread URL 알 필요 X.)

### 위임 후 메인이 **하지 말 것** (중요)
- ❌ "기다리다 못해" 자기 도구(`write_file`, `terminal`, `read_file`)로 같은 작업을 *대신* 실행. **이중 작업 + 결과 충돌**.
- ❌ "코더가 끝났나?" 폴링 (`for i in $(seq 1 20); do ... done`).
- ❌ "Spawned run X" 한 줄만 보내고 끝내기.
- ❌ 코더 thread URL/run_id 만 던지기.

### 위임 후 메인이 **할 것**
- 본 채널에서 다른 대화 자연스럽게 계속.
- 사용자가 본 채널에서 "코더 진행 어떻게 됐어?" 물으면 → "스레드에서 확인 가능합니다" 한 줄 또는 `active_subagents` (있다면) 호출.
- 코더가 끝나면 (✅ 완료 메시지가 thread에 뜸) 메인은 별도 ping 안 함 — thread 알림으로 충분.

### 헷갈리는 경계 케이스
메인이 직접 답하는 쪽으로 bias. 답한 뒤 한 줄:
> "코더에게 강제로 넘기려면 `/code <요청>` 으로 호출해 주세요."

### Codex 환경 메모
- sandbox 모드: `danger-full-access` (이 VM 환경에서 workspace-write bwrap이 깨짐).
- 자식 LLM 클라이언트는 `CodexExecFacade`. NDJSON 이벤트 → 부모 `tool_progress_callback("subagent_progress", …)` → Discord thread.
- `sessions_spawn`(ACP) 와 다른 경로 — 이건 `delegate_task_background` (registry tool). 위 ACP 가이드와 별도.
```

**new_string** (V2):

```markdown
## 🧑‍💻 Coding Delegation

Discord 봇에서 코드 작성·실행 요청은 별도의 코더 자식(Codex CLI)에게 위임. 메인 대화 컨텍스트를 코드 디테일로 채우지 않는다.

### 핵심 룰

이 작업을 완료하려면 도구를 **2회 이상 호출**해야 하는가?
- **예** (read→edit, edit→verify, test→iterate, 여러 파일, debug-loop) → `delegate_task_background`로 위임
- **아니오** (단발성 write_file 1회, 또는 응답만) → in-turn OK

자기검증 질문: "이 작업을 완료하려면 read_file을 먼저 해야 하나? terminal로 실행해서 결과를 확인해야 하나? write_file을 두 번 이상 해야 하나?" → yes면 위임.

### 경계 케이스 bias: 헷갈리면 **위임**

비용 비대칭이라 경계 케이스는 위임 쪽으로 기울임.
- 위임이 잘못된 선택 = thread 하나 추가 + 약간의 사용자 인지 부담
- in-turn이 잘못된 선택 = main 컨텍스트 오염, 작업 성능 저하, 감사 기록 부재

### 경계 예시

| 사용자 요청 | 판정 | 이유 |
|---|---|---|
| "한 줄 print 추가" | in-turn OK | write_file 1회 |
| "함수 X 추가하고 호출도 넣어줘" | **위임** | read → edit → verify (multi-step) |
| "이 파일에서 OAuth 부분 5줄 설명해줘" | in-turn OK | read 1회 + 답변, 변경 없음 |
| "에러 메시지 보고 고쳐줘" | **위임** | read → 진단 → edit → 재실행 |
| "테스트 작성하고 통과시켜줘" | **위임** | write → run → iterate |
| "이 한 줄 syntax 맞아?" | in-turn OK | 응답만 |
| "여러 파일에 같은 import 추가" | **위임** | 여러 파일 = multi-step |
| "리팩토링/이름 변경/모듈 정리" | **위임** | 항상 multi-step |
| "내가 방금 만든 파일 살짝만 수정 (1줄)" | in-turn OK | 사용자가 명시적으로 단일 변경 |

### 수정 없는 분석/탐색은 main이 직접

핵심 룰("2+ 도구 = 위임")은 *수정 없는 분석*에는 적용 안 됨. 분석은 사용자와의 **대화의 일부**이므로 main이 직접 처리. follow-up과 연결되어야 하니까.

| 요청 | 판정 | 이유 |
|---|---|---|
| "이 함수 어떻게 작동하는지 설명해줘" | in-turn | 답변 = 채널 메시지, 대화 흐름 |
| "OAuth 부분 코드 분석" | in-turn | 분석 결과 → main이 직접 답해야 follow-up |
| "전체 모듈 구조를 markdown으로 /tmp/analysis.md에 정리" | **위임** | deliverable이 파일 |
| "버그 찾아서 고쳐줘" | **위임** | 분석 + 수정 (수정 들어가면 coder) |

**기준 한 줄**: 수정 또는 파일 산출물이 있나? 있으면 coder, 없으면 main.

### 사용자 명시 override

핵심 룰에도 불구하고 사용자 명시 지시가 우선.

in-turn 명시 트리거 예시:
- "thread 말고 직접 고쳐줘"
- "그냥 빠르게 수정해"
- "분석한 결과 보고 같이 고치자"
- "코더 말고 네가 해"

반대로 "코더로 보내줘", "thread에서 처리해줘"라고 하면 trivial 작업도 **위임**.

명시 신호 없을 때만 위 룰 적용.

### 복잡도 초과 안내

in-turn으로 시작한 작업이 도구 호출 **5회 이상**에 도달하면 사용자에게 한 줄:
> "이 작업이 예상보다 복잡해지고 있습니다. 코더로 넘기시겠어요? `/code <요청>`"

안내만 출력. 응답 기다리지 말고 현재 도구 호출은 계속. 사용자가 다음 메시지에서 코더 명시 요청 시 그때 위임.

### `delegate_task` vs `delegate_task_background` 역할 분리

| 도구 | 용도 | 결과 경로 |
|---|---|---|
| `delegate_task` (sync) | **비-코딩 위임**: 리서치, 분석, 멀티스텝 일반, 외부 정보 수집 | main이 받아 채널에 답변 (max 24K 토큰) |
| `delegate_task_background` (async) | **코딩 작업 전용**: 파일 수정, 디버깅, 테스트, 리팩토링, 빌드 실행 | 자동 생성된 Discord thread |

- **코딩에 `delegate_task`(sync) 절대 쓰지 말 것**. 결과를 thread로 보낼 채널이 없어 main 컨텍스트 오염 + 가시성 손실 + cancel 같은 코더 인프라 못 씀.
- **비-코딩에 `delegate_task_background` 절대 쓰지 말 것**. codex CLI는 코딩 환경(workspace, sandbox, file ops)에 맞춰져있고 일반 작업에 부적합.

### 호출 패턴

```
delegate_task_background(
    goal="자기완결적 한 줄 요약 — 자식은 대화 history를 모름",
    context="관련 파일 경로, 에러 메시지, 제약 (간결하게)"
)
```

호출 직후 사용자에게 보낼 응답 한 줄:
> "▶ 코더에게 위임 — 진행은 자동 생성된 스레드 참조"

(앵커 메시지 + Discord 스레드는 자동 생성됨. 메인이 thread URL 알 필요 X.)

### 위임 후 메인이 **하지 말 것**
- ❌ "기다리다 못해" 자기 도구(`write_file`, `terminal`, `read_file`)로 같은 작업을 *대신* 실행. **이중 작업 + 결과 충돌**.
- ❌ "코더가 끝났나?" 폴링 (`for i in $(seq 1 20); do ... done`).
- ❌ "Spawned run X" 한 줄만 보내고 끝내기.
- ❌ 코더 thread URL/run_id 만 던지기.

### 위임 후 메인이 **할 것**
- 본 채널에서 다른 대화 자연스럽게 계속.
- "코더 진행?" 질문 → "스레드 확인 가능" 한 줄 또는 `active_subagents` 호출.
- 코더 완료 시 별도 ping 안 함 — thread 알림으로 충분.

### Codex 환경 메모
- sandbox 모드: `danger-full-access` (이 VM 환경에서 workspace-write bwrap이 깨짐).
- 자식 LLM 클라이언트는 `CodexExecFacade`. NDJSON 이벤트 → 부모 `tool_progress_callback("subagent_progress", …)` → Discord thread.
- `sessions_spawn`(ACP)는 다른 경로 — `delegate_task_background`은 registry tool. ACP 가이드와 별도.
- 사용자가 코더 thread에서 `!cancel` 또는 `!stop` 입력 시 codex 프로세스 그룹 SIGTERM 후 ❌ 취소됨 표시.
```

- [ ] **Step 2: Spot-check the edit applied cleanly**

Run:
```bash
grep -n "## 🧑‍💻 Coding Delegation\|핵심 룰\|## Red Lines" ~/.hermes/workspace/AGENTS.md
```

Expected output (with new line numbers):
```
132:## 🧑‍💻 Coding Delegation
136:### 핵심 룰
...
???:## Red Lines
```

The exact line for `## Red Lines` shifts (V2 body is slightly different length from V1), but it must still exist and follow the V2 section.

- [ ] **Step 3: Verify the file is still well-formed Markdown**

Run:
```bash
head -5 ~/.hermes/workspace/AGENTS.md
tail -5 ~/.hermes/workspace/AGENTS.md
wc -l ~/.hermes/workspace/AGENTS.md
```

Expected: file isn't truncated, frontmatter (if any) intact, no missing `## Red Lines` section, and total line count is plausible (similar order of magnitude to V1).

---

## Task 2: Live verify — multi-step rule (Scenario 1 re-run)

This is the test the V1 failed. Re-run with V2 in place.

**Files:** none modified. Only verification.

- [ ] **Step 1: In Discord, post the Scenario 1 prompt**

Send to the main hermes channel (where bot listens to mentions):
```
@Hermes /tmp/scratch_e2e_v2.py에 hello world 한 줄 print 추가하고 호출도 해줘
```

- [ ] **Step 2: Observe parent's behavior**

PASS criteria (V2 working): parent posts `▶ 코더에게 위임 — coder-XXXXXXXX` and a coder thread is created. Inside the thread, codex events flow (`▶️ ...`, agent_message, `✅ 완료`), file is written.

FAIL criteria (V2 still too weak): parent calls `write_file` in-turn (anchor message shows `✍️ write_file: "/tmp/..."` instead of the delegation line). Note: still functionally fine, but means V2's "헷갈리면 위임" bias is not yet strong enough.

- [ ] **Step 3: Record the outcome**

If PASS: continue to Task 3.

If FAIL: stop the plan, surface the result to the operator. Possible next moves: (a) sharpen the boundary examples (e.g. add the literal prompt as an example row), (b) revisit whether tool removal from `_HERMES_CORE_TOOLS` is needed after all (escalate, this plan does not authorize that change).

---

## Task 3: Live verify — analysis exception

Confirm the V2 carve-out for read-only analysis still routes to main, not coder.

**Files:** none modified.

- [ ] **Step 1: In Discord, send a pure analysis request**

Send to the main hermes channel:
```
@Hermes ~/.hermes/hermes-agent/agent/codex_exec_client.py 의 CodexExecFacade 클래스 역할 5줄로 설명해줘
```

- [ ] **Step 2: Observe**

PASS: parent reads the file (1 tool call, possibly grep+read = 2) and answers in the channel directly. **No thread**, no `▶ 코더에게 위임` line.

FAIL: parent delegates to coder thread for what is read-only multi-step analysis.

- [ ] **Step 3: Record outcome**

If PASS: continue.
If FAIL: the analysis exception language in V2 is being shadowed by the multi-step rule. Surface to operator; possible refinement: hoist the analysis exception above the multi-step rule, or rephrase the rule to "수정 또는 산출물 필요한 작업이 2+ 도구면…".

---

## Task 4: Live verify — user override

Confirm explicit user instruction beats the rule.

**Files:** none modified.

- [ ] **Step 1: In Discord, send an override-style request**

Send to the main hermes channel:
```
@Hermes thread 말고 그냥 직접 /tmp/override_e2e.py에 print("hi") 한 줄짜리 main 함수 만들고 호출까지 넣어줘
```

This prompt would otherwise be multi-step (write_function + verify), but the leading `thread 말고 그냥 직접` is the override trigger.

- [ ] **Step 2: Observe**

PASS: parent does it in-turn with `write_file` (anchor `✍️ write_file: "/tmp/override_e2e.py"`), no coder thread.

FAIL: parent ignores the override and delegates. Means override language is not being honored — V2 wording around override probably needs to be moved earlier or made more explicit.

- [ ] **Step 3: Record outcome**

If PASS: continue.
If FAIL: surface, do not block on this. The override path is a quality-of-life feature, not a correctness gate.

---

## Task 5: Regression check — V1-validated scenarios still work

Sanity check the wider coder pipeline didn't regress. AGENTS.md V2 is text-only but the parent's interpretation could indirectly affect existing flows.

**Files:** none modified.

- [ ] **Step 1: `/code` still works**

In Discord:
```
/code /tmp/v2_regression_a.py에 print("a") 한 줄
```

Expected: thread created, codex runs, file written. Identical to Task 13 Scenario 2 (already PASS at branch SHA `7a32d2cfc`).

- [ ] **Step 2: `!cancel` still works**

In Discord:
```
/code 30초간 매 초마다 sleep하고 hello 출력 (bash로 직접)
```

Then inside the spawned thread:
```
!cancel
```

Expected: `❌ 취소됨` posted, `pgrep -af codex` returns nothing. Identical to Task 13 Scenario 6.

- [ ] **Step 3: Follow-up via `codex exec resume` still works**

In the thread from Step 1 (still active):
```
"a" 옆에 "b"도 추가해줘
```

Expected: same thread receives follow-up codex events, file becomes `print("ab")` or similar. Identical to Task 13 Scenario 5.

- [ ] **Step 4: Record outcomes**

If all 3 PASS: continue to commit.
If any FAIL: rollback via Task 0 Step 3 command, investigate, do NOT commit V2.

---

## Task 6: Commit + finishing memo update

**Files:**
- Modify (commit): `~/.hermes/workspace/AGENTS.md`
- Modify: `~/.claude/projects/-home-bykim0119-autonormal/memory/project_hermes_coder_subagent.md`

Note: `~/.hermes/workspace/` is not necessarily under git tracking. Confirm before committing.

- [ ] **Step 1: Determine workspace git status**

Run:
```bash
cd ~/.hermes/workspace && git status 2>&1 | head -5
```

If output begins with `fatal: not a git repository` → workspace is not under git. Skip git commit for the workspace; the backup from Task 0 is the only rollback path.

If it IS a git repo: stage and commit:
```bash
cd ~/.hermes/workspace
git add AGENTS.md
git commit -m "docs(agents): V2 coding-delegation rule rewrite (multi-step + reversed bias)"
```

- [ ] **Step 2: Update the hermes-coder-subagent memory note**

Edit `~/.claude/projects/-home-bykim0119-autonormal/memory/project_hermes_coder_subagent.md`. Locate the section that lists Task 9 AGENTS.md status (current text mentions "Task 9 AGENTS.md guide" and "V1 수용, 더 강한 억제는 V2(키워드 라우터)로"). Append a new dated note:

```markdown
### 2026-05-21 AGENTS.md V2 적용
- spec: `docs/superpowers/specs/2026-05-21-agents-md-v2-design.md`
- plan: `docs/superpowers/plans/2026-05-21-agents-md-v2.md`
- 변경 핵심: 트리거 리스트 → 핵심 룰("2+ 도구 = 위임") + 헷갈리면 위임 bias 반전 + 분석 예외 명시 + 사용자 override + `delegate_task` vs `delegate_task_background` 역할 분리
- 라이브 검증: Scenario 1 재실행 결과 [PASS/FAIL 기록], 분석 예외 [PASS/FAIL], 사용자 override [PASS/FAIL], 회귀 (Scenarios 2/5/6) [PASS/FAIL]
- 백업: `~/migration-backups/agents-md-v2-<TS>.bak`
```

Replace the `[PASS/FAIL 기록]` placeholders with the actual outcomes from Tasks 2–5.

- [ ] **Step 3: Mark plan complete**

If the operator uses a task tracker (TaskUpdate), mark the AGENTS.md V2 task completed.

---

## Self-review

Spec coverage:
- Section 1 (Core Rule) → Task 1 new_string
- Section 2 (Bias reversal) → Task 1 new_string
- Section 3 (Boundary examples + analysis exception) → Task 1 new_string, verified Task 3
- Section 4 (User override + escalation) → Task 1 new_string, verified Task 4
- Section 5 (Tool role split) → Task 1 new_string
- Section 6 (V1 retained pieces) → Task 1 new_string preserves them
- Section 7 (Migration) → Tasks 0, 1, 6

Testing per spec: Task 2 = Scenario 1 re-run, Task 5 = regression. Covered.

Out of scope per spec (toolsets.py, code changes): plan does not touch them. Covered.
